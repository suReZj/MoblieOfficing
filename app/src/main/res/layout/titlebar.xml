<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="81dp"
    android:paddingTop="@dimen/padding_top"
    android:background="@drawable/basebottomline"
    >

    <TextView
        android:id="@+id/toolbar__title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:gravity="center"
        android:textSize="20sp"
        android:visibility="gone"
        />

    <Button
        android:id="@+id/toolbar_leftButton"
        android:layout_width="64dp"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:paddingLeft="6dp"
        android:drawableLeft="@mipmap/ic_toolbar_return"
        android:drawablePadding="-12dp"
        android:background="@color/transparent"
        android:textSize="14sp"
        android:visibility="gone"
        />

    <Button
        android:id="@+id/toolbar_rightSecondButton"
        android:layout_width="52dp"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:background="@color/transparent"
        android:textSize="14sp"
        android:gravity="center_vertical|right"
        android:paddingRight="6dp"
        android:visibility="gone"
        />

    <Button
        android:id="@+id/toolbar_rightFirstButton"
        android:layout_width="52dp"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/toolbar_rightSecondButton"
        android:textSize="14sp"
        android:background="@color/transparent"
        android:layout_centerVertical="true"
        android:gravity="center_vertical|right"
        android:paddingRight="6dp"
        android:visibility="gone"
        />

</RelativeLayout>